apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../env/labels

resources:
  - namespace.yaml
  - ../env

namespace: desarrollo-portal

commonLabels:
  workshop.openshift.io/proyecto: portal
