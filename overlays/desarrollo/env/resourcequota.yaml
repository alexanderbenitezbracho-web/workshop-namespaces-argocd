apiVersion: v1
kind: ResourceQuota
metadata:
  name: workshop-quota
spec:
  hard:
    count/deployments.apps: "2"
    requests.cpu: "1"
    limits.cpu: "2"
    requests.memory: 500Mi
    limits.memory: 2Gi
    requests.storage: 2Gi
    pods: "2"
    
