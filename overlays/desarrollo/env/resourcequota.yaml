apiVersion: v1
kind: ResourceQuota
metadata:
  name: workshop-quota
spec:
  hard:
    requests.cpu: "600m"
    limits.cpu: "1"
    requests.memory: 1Gi
    limits.memory: 2Gi
    requests.storage: 2Gi
