apiVersion: v1
kind: ResourceQuota
metadata:
  name: workshop-quota
spec:
  hard:
    requests.cpu: "3500m"
    limits.cpu: "4"
    requests.memory: 10Gi
    limits.memory: 16Gi
    requests.storage: 20Gi
