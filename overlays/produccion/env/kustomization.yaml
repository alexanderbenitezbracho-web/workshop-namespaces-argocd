apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base
  - limitrange.yaml
  - resourcequota.yaml
  - networkpolicy-allow-same-entorno.yaml

commonLabels:
  workshop.openshift.io/entorno: produccion
